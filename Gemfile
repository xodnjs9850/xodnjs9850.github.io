source "https://rubygems.org"

# GitHub Pages가 빌드 시 사용하는 Gem 묶음 (로컬 미리보기용)
gem "github-pages", group: :jekyll_plugins

gem "jekyll-remote-theme"
gem "jekyll-seo-tag"

# Windows / JRuby 보조
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
